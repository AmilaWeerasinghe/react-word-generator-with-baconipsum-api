import React, { Component } from "react";

export class select extends Component {
  constructor(props) {
    super(props);
    this.state = {
      value: props.value
    };
  }
  render() {
    return <div></div>;
  }
}

export default select;
